{{- $otel := .Values.topology.otel | default dict -}}
{{- if not (kindIs "map" $otel) -}}
{{- fail "topology.otel must be a map" -}}
{{- end -}}
{{- if get $otel "enabled" }}
{{- $otelName := include "nemo-retriever.otel.fullname" . -}}
{{- $otelConfigName := include "nemo-retriever.otel.configMapName" . -}}
{{- $otelPorts := include "nemo-retriever.otel.ports" . | fromYaml -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $otelName }}
  labels:
    {{- include "nemo-retriever.role.labels" (dict "context" $ "role" "otel") | nindent 4 }}
spec:
  replicas: {{ $otel.replicas }}
  selector:
    matchLabels:
      {{- include "nemo-retriever.role.selectorLabels" (dict "context" $ "role" "otel") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nemo-retriever.role.selectorLabels" (dict "context" $ "role" "otel") | nindent 8 }}
      annotations:
        checksum/otel-config: {{ include (print $.Template.BasePath "/configmap-otel.yaml") . | sha256sum }}
    spec:
      {{- include "nemo-retriever.imagePullSecrets" . | nindent 6 }}
      {{- with $otel.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $otel.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $otel.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: otel-collector
          image: "{{ $otel.image.repository }}:{{ $otel.image.tag }}"
          imagePullPolicy: {{ $otel.image.pullPolicy }}
          args:
            - --config=/etc/otelcol/config.yaml
          ports:
            - name: otlp-grpc
              containerPort: {{ get $otelPorts "otlpGrpc" }}
              protocol: TCP
            - name: otlp-http
              containerPort: {{ get $otelPorts "otlpHttp" }}
              protocol: TCP
            - name: prometheus
              containerPort: {{ get $otelPorts "prometheus" }}
              protocol: TCP
          resources:
            {{- toYaml $otel.resources | nindent 12 }}
          volumeMounts:
            - name: otel-config
              mountPath: /etc/otelcol
              readOnly: true
          livenessProbe:
            httpGet:
              path: /
              port: 13133
            initialDelaySeconds: 5
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 13133
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: otel-config
          configMap:
            name: {{ $otelConfigName }}
{{- end }}
