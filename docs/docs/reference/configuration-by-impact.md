<!-- Generated by docs/scripts/generate_config_docs.py; do not edit by hand. -->


# Configuration by impact

Index of `@configured` resolver sites grouped by impact tag.

## PERFORMANCE

- **`pipeline_defaults.embed`** — `nemo_retriever.service.services.pipeline_executor.build_embed_params`: Remote embed NIMs scale horizontally; local HF embedders reduce latency but consume VRAM per worker.

## ACCURACY

- **`pipeline_defaults.extract`** — `nemo_retriever.service.services.pipeline_executor.build_extract_params`: OCR version and table-structure toggles trade extraction quality against per-page latency; NIM URLs shift work off-pod.
- **`pipeline_defaults.caption`** — `nemo_retriever.service.services.pipeline_executor.build_caption_params`: Remote VLM captioning improves multimodal coverage at the cost of NIM spend.

## THROUGHPUT

_No registered resolvers._

## LATENCY

- **`pipeline_defaults.extract`** — `nemo_retriever.service.services.pipeline_executor.build_extract_params`: OCR version and table-structure toggles trade extraction quality against per-page latency; NIM URLs shift work off-pod.
- **`pipeline_defaults.asr`** — `nemo_retriever.service.services.pipeline_executor.build_asr_params`: Remote ASR avoids loading Parakeet in-pod; local ASR reduces network hops but uses GPU memory.

## MEMORY

- **`pipeline_defaults.asr`** — `nemo_retriever.service.services.pipeline_executor.build_asr_params`: Remote ASR avoids loading Parakeet in-pod; local ASR reduces network hops but uses GPU memory.
- **`pipeline_defaults.embed`** — `nemo_retriever.service.services.pipeline_executor.build_embed_params`: Remote embed NIMs scale horizontally; local HF embedders reduce latency but consume VRAM per worker.

## COST

- **`pipeline_defaults.caption`** — `nemo_retriever.service.services.pipeline_executor.build_caption_params`: Remote VLM captioning improves multimodal coverage at the cost of NIM spend.
- **`pipeline_defaults.embed`** — `nemo_retriever.service.services.pipeline_executor.build_embed_params`: Remote embed NIMs scale horizontally; local HF embedders reduce latency but consume VRAM per worker.

## RELIABILITY

_No registered resolvers._

## SECURITY

_No registered resolvers._

## COMPATIBILITY

_No registered resolvers._
