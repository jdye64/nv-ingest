<!-- Generated by docs/scripts/generate_config_docs.py; do not edit by hand. -->


# Pipeline defaults (`pipeline_defaults`)

SDK pipeline defaults merged with service NIM/local resolution.

    Values here apply to inprocess, batch, and service modes unless
    overridden at call time or via per-request ``PipelineSpec``.

### pipeline_defaults

SDK pipeline defaults merged with service NIM/local resolution.

    Values here apply to inprocess, batch, and service modes unless
    overridden at call time or via per-request ``PipelineSpec``.

| Field | Type | Default | Description | Impact |
|-------|------|---------|-------------|--------|
| `pipeline_defaults.extract` | `ExtractParams` | `PydanticUndefined` | — | — |
| `pipeline_defaults.embed` | `EmbedParams` | `PydanticUndefined` | — | — |
| `pipeline_defaults.asr` | `ASRParams` | `PydanticUndefined` | — | — |
| `pipeline_defaults.caption` | `CaptionParams` | `PydanticUndefined` | — | — |
| `pipeline_defaults.store` | `StoreParams` | `PydanticUndefined` | — | — |
| `pipeline_defaults.dedup` | `DedupParams` | `PydanticUndefined` | — | — |

## Configured resolvers

| Function | Section | Variants | Impact | Rationale |
|----------|---------|----------|--------|-----------|
| `nemo_retriever.service.services.pipeline_executor.build_extract_params` | `pipeline_defaults.extract` | nim, local | ACCURACY, LATENCY | OCR version and table-structure toggles trade extraction quality against per-page latency; NIM URLs shift work off-pod. |
| `nemo_retriever.service.services.pipeline_executor.build_caption_params` | `pipeline_defaults.caption` | nim | ACCURACY, COST | Remote VLM captioning improves multimodal coverage at the cost of NIM spend. |
| `nemo_retriever.service.services.pipeline_executor.build_asr_params` | `pipeline_defaults.asr` | nim, local | LATENCY, MEMORY | Remote ASR avoids loading Parakeet in-pod; local ASR reduces network hops but uses GPU memory. |
| `nemo_retriever.service.services.pipeline_executor.build_embed_params` | `pipeline_defaults.embed` | nim, local | PERFORMANCE, MEMORY, COST | Remote embed NIMs scale horizontally; local HF embedders reduce latency but consume VRAM per worker. |
